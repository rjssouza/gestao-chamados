import { PainelAreaPipe } from './painel-area.pipe';

describe('PainelAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new PainelAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
